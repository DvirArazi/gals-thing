# Copy this file to `.env.local` if you are not using `vercel env pull .env.local`.
# The app also accepts `POSTGRES_URL`, `POSTGRES_URL_NON_POOLING`, and `NEON_DATABASE_URL`.
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
